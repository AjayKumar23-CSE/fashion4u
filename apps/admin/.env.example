VITE_API_URL="http://localhost:4000/api/v1"
# Seed images are stored as storefront-relative paths (/seed/...); this is
# where the admin loads them from. Uploaded images are full S3 URLs.
VITE_STOREFRONT_URL="http://localhost:3000"
